knife4j:
  enable: true
  openapi:
    description: Groot_worker测试
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.itheima.test.controller
        group-name: default
    title: Groot_worker测试
    version: v1.0.0
    url: hello

  setting:
    enable-document-manage: false
    enable-footer-custom: true
    enable-group: false
    enable-swagger-models: false
    footer-custom-content: Groot-worker测试
spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
#  redis:
#    database: 0
#    host: localhost
#    jedis:
#      pool:
#        max-active: 8
#        max-idle: 8
#        max-wait: -1ms
#        min-idle: 0
#    port: 6379
